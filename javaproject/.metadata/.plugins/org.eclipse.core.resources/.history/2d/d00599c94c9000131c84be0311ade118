import java.sql.*;

public class ecodb {

	public ecodb(){
		Connection conn = null;
		try{
			conn = DriverManager.getConnection("jdbc:sqlite:test.db");
		}catch(Exception e){
			System.err.println(e);
		}
	}
}
